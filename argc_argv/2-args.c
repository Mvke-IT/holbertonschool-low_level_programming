#include <stdio.h>
/**
 * main - starts the program
 * @argc: size of the string
 * @argv: contains the programm command
 * Return: Return 0
 */

int main(int argc, char *argv[])
{
	int i;

	for(i = 0; 1 < argc; i++0)
	{
		printf("%s\n", argv [i]);
	}
	reutrn 0;
}
