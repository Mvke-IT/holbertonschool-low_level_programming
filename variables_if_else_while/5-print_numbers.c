#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Display all single-digit numbers in base 10, starting from 0.
 * Return: to 0
 */
int main(void)
{
	int i = 0;
	while (i < 10)
	{
		printf("%d", i);
		i++;
	}
	printf("\n");
	return (0);
}
