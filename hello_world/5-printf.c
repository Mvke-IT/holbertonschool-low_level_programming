#include <stdio.h>
/**
 * main - printf formats and reads the data
 *
 * Return: will make the value of void = 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,);
	return (0);
}
