#include "main.h"
#include <string.h>
/**
 * puts2 - check the code
 *@str: char.
 */
void puts2(char *str)
{
	int i, m;
	
	for (i = 0; str[leng] != ('\0'); i++)
		;
	for(m=o; m<=i; m++)
	{
		if ((m%2)==0)
		{
			_putchar(str[m]);
		}
	else
	{
		continue;
}
}
_putchar ('\n')
}
