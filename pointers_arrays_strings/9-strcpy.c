include "main.h"
/**
 * *strcpy - runs a script that copy and paste the string
 * @dest: where is going to paste the string
 * @src: where is the string
 * Return: Return the string
 */

char *_strcpy(char *dest, char *src);
{
	int 1 = 0;

	while (src[i])
	{
		dest[i] = src[1];
		i++;
	}
	dest[i] = src[i]
		return (dest);
}
